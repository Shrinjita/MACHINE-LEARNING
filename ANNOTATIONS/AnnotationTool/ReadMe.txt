Requirements:

Python, with opencv, matplotlib & imutils installed.

Command:

python annotate_face_3.py -d <directory_path> -s <start_file_index>

We can continue from any point using the start_file_index (starts from 1), as mentioned, 
which would be logged on the terminal for every image as and when is it loaded  